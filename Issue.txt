Can you update below mapRequestEntity please according to the entity I provided and also PostRequest and also suggest necessary changes to be done in other files.
package com.sc.scroe.masteraccount.util;

import com.sc.scroe.masteraccount.entity.SubledgerMasteraccountEntity;
import com.sc.scroe.masteraccount.request.MasterAccountPostRequest;
import org.springframework.stereotype.Component;

@Component
public class SubledgerMasterAccountUtil {

    private void mapPostRequestToEntity(
            MasterAccountPostRequest request,
            SubledgerMasteraccountEntity entity) {

        entity.setDealId(request.getDealId());
        entity.setClientId(request.getClientId());

        entity.setMasterAccountNumber(request.getMasterAccountNumber());
        entity.setMasterAccountCountry(request.getMasterAccountCountry());
        entity.setMasterAccountCurrencyCode(request.getMasterAccountCurrencyCode());

        entity.setMasterAccountName(request.getMasterAccountName());
        entity.setCompanyInitial(request.getCompanyInitial());
        entity.setClientType(request.getClientType());
        entity.setSubAccountType(request.getSubAccountType());
        entity.setProductCode(request.getProductCode());

        entity.setAccountClassificationCode(
                request.getAccountClassificationCode());

        entity.setSubKycCddFlag(request.getSubKycCddFlag());

        entity.setSubKycCddRiskCode(
                request.getSubKycCddRiskCode());

        entity.setSubDefaultRiskCodeHoldFlag(
                request.getSubDefaultRiskCodeHoldFlag());

        entity.setSubDefaultRiskCode(
                request.getSubDefaultRiskCode() == null
                        ? null
                        : String.join(",",
                        request.getSubDefaultRiskCode()));

        entity.setMasterAccountAddressInheritFlag(
                request.getMasterAccountAddressInheritFlag());

        entity.setClientsMoney(request.getClientsMoney());

        entity.setInfoCodeClientsMoney(
                request.getInfoCodeClientsMoney());

        entity.setSubAccountClosureOffset(
                request.getSubAccountClosureOffset());

        if (request.getMiscInfos() != null) {

            entity.setMiscInfos(
                    new ObjectMapper()
                            .writeValueAsString(
                                    request.getMiscInfos()));
        }

        entity.setAccountBranch(request.getAccountBranch());
        entity.setConsolidatedStatementFlag(
                request.getConsolidatedStatementFlag());

        entity.setGlid(request.getGlid());

        entity.setMasterNumber(request.getMasterNumber());

        entity.setAccountCategory(
                request.getAccountCategory());

        entity.setDsrClosingId(
                request.getDsrClosingId());

        entity.setAdviseInterestIndicator(
                request.getAdviseInterestIndicator());

        entity.setIbanNumber(
                request.getIbanNumber());

        entity.setCloseDate(request.getCloseDate());

        entity.setSubProductCode(
                request.getSubProductCode());

        entity.setCreditInterestCode(
                request.getCreditInterestCode());

        entity.setDebitInterestCode(
                request.getDebitInterestCode());

        entity.setInterestCode(
                request.getInterestCode());

        entity.setSuppressStatementFlag(
                request.getSuppressStatementFlag());

        entity.setCreditSubAccountInterestToMasterAccount(
                request.getCreditSubAccountInterestToMasterAccount());

        entity.setMaxSubAccountLimit(
                request.getMaxSubAccountLimit());

        entity.setOpenedOn(request.getOpenedOn());

        entity.setEbbsAccountStatus(
                request.getEbbsAccountStatus());
    }
}

package com.sc.scroe.masteraccount.request;

import java.time.LocalDate;
import java.util.List;

import com.fasterxml.jackson.annotation.JsonFormat;
import jakarta.validation.constraints.NotBlank;
import lombok.Data;

@Data
public class MasterAccountPostRequest {

    @NotBlank
    private String dealId;

    @NotBlank
    private String clientId;

    @NotBlank
    private String masterAccountNumber;

    @NotBlank
    private String masterAccountCountry;

    @NotBlank
    private String masterAccountCurrencyCode;

    private String masterAccountName;
    private String companyInitial;
    private String clientType;
    private String subAccountType;
    private String productCode;
    private String accountClassificationCode;
    private String subKycCddFlag;
    private String subKycCddRiskCode;
    private String subDefaultRiskCodeHoldFlag;

    private List<String> subDefaultRiskCode;

    private String masterAccountAddressInheritFlag;
    private String clientsMoney;
    private String infoCodeClientsMoney;
    private String subAccountClosureOffset;

    private List<MiscInfoDTO> miscInfos;

    private String accountBranch;
    private String consolidatedStatementFlag;
    private String glid;

    @NotBlank
    private String masterNumber;

    private String accountCategory;
    private String dsrClosingId;
    private String adviseInterestIndicator;
    private String ibanNumber;

    @JsonFormat(pattern = "yyyy-MM-dd")
    private LocalDate closeDate;

    private String subProductCode;
    private String creditInterestCode;
    private String debitInterestCode;
    private String interestCode;
    private String suppressStatementFlag;
    private String creditSubAccountInterestToMasterAccount;
    private String maxSubAccountLimit;

    @JsonFormat(pattern = "yyyy-MM-dd")
    private LocalDate openedOn;

    private String ebbsAccountStatus;
}



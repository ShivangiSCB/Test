package com.scb.scroeconnect.solaceworker.util;

import com.scb.scroeconnect.solaceworker.entity.ConnectNotificationDelivery;
import com.scb.scroeconnect.solaceworker.entity.ConnectSolaceWorker;
import com.scb.scroeconnect.solaceworker.entity.NotificationEvent;
import com.scb.scroeconnect.solaceworker.repository.ConnectNotificationEventRepository;
import com.scb.scroeconnect.solaceworker.repository.ConnectSolaceWorkerRepository;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Component;
import tools.jackson.databind.ObjectMapper;

import java.time.ZonedDateTime;

@Component
public class SaveFinalPayloadUtil
{
        @Autowired
        ConnectNotificationEventRepository connectNotificationEventRepository;

        @Autowired
        ConnectSolaceWorkerRepository connectSolaceWorkerRepository;

        private static final ObjectMapper mapper = new ObjectMapper();

    public void saveFinalPayload(ConnectNotificationDelivery delivery, Object finalPayload, String deliveryMode) throws RuntimeException {
        NotificationEvent notificationEvent = connectNotificationEventRepository
                .findByEventId(delivery.getNotificationEvent().getEventId())
                .orElseThrow(() -> new RuntimeException("ALERT TO PSS, Event not found for ID: " + delivery.getNotificationEvent().getEventId()));

        // Create and populate the ConnectNotificationEmailInfo entity
        ConnectSolaceWorker connectSolaceWorker = new ConnectSolaceWorker();
        connectSolaceWorker.setDeliveryMode(deliveryMode);
        connectSolaceWorker.setNotificationEvent(notificationEvent);
        connectSolaceWorker.setFinalPayload(mapper.valueToTree(finalPayload));
        connectSolaceWorker.setSendAt(ZonedDateTime.now());
        connectSolaceWorker.setNotificationDelivery(delivery);

        // Save the entity
        connectSolaceWorkerRepository.save(connectSolaceWorker);
    }

}

package com.scb.scroeconnect.solaceworker.service;

import com.scb.scroeconnect.solaceworker.dto.FinalPayloadDto.FinalWebhookPayloadDto;
import com.scb.scroeconnect.solaceworker.dto.TransformedPayloadDto.TransformedWebhookPayloadDto;
import com.scb.scroeconnect.solaceworker.entity.ConnectSolaceConfig;
import com.scb.scroeconnect.solaceworker.enums.DeliveryStatus;
import com.scb.scroeconnect.solaceworker.mapper.FinalPayloadMapper;
import com.scb.scroeconnect.solaceworker.mapper.FinalWebhookPayloadMapper;
import com.scb.scroeconnect.solaceworker.repository.ConnectSolaceConfigRepository;
import com.scb.scroeconnect.solaceworker.util.*;
import lombok.AllArgsConstructor;
import lombok.extern.log4j.Log4j2;
import com.scb.scroeconnect.solaceworker.dto.FinalPayloadDto.FinalPayloadDto;
import com.scb.scroeconnect.solaceworker.dto.TransformedPayloadDto.TransformedPayloadDto;
import com.scb.scroeconnect.solaceworker.entity.ConnectNotificationDelivery;
import com.scb.scroeconnect.solaceworker.repository.ConnectNotificationDeliveryRepository;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.beans.factory.annotation.Value;
import org.springframework.stereotype.Service;
import org.springframework.transaction.annotation.Transactional;
import tools.jackson.databind.JsonNode;

import java.util.Optional;
import java.util.UUID;

@Log4j2
@Service
@AllArgsConstructor
public class SolaceWorkerService {

    private final ConnectNotificationDeliveryRepository connectNotificationDeliveryRepository;
    @Autowired
    private final RetryHandler retryHandler;
    private final PublishToSolaceUtil publishToSolace;
    private final ParseTransformedPayloadUtil parseTransformedPayload;
    private final SaveFinalPayloadUtil saveFinalPayloadUtil;
    private final FinalPayloadMapper finalPayloadMapper;
    private final ConnectSolaceConfigRepository connectSolaceConfigRepository;
    private final FinalWebhookPayloadMapper finalWebhookPayloadMapper;
    private final PublishToWebhookUtil publishToWebhook;

    @Value("${solace.s2bl.recipientId}")
    private final int solaceRecipientId;

    @Value("${solace.webhook.recipientId}")
    private final int webhookRecipientId;

    @Transactional
    public void sendSolaceNotification(ConnectNotificationDelivery delivery) {
        try {
            log.info("Processing Notification for Delivery ID: {}", delivery.getId());

            JsonNode transformedPayload = delivery.getPayload();

            Long recipientId = delivery.getRecipient().getId();
            UUID eventId = delivery.getNotificationEvent().getEventId();

            Optional<ConnectSolaceConfig> configOpt = connectSolaceConfigRepository.findByRecipientIdAndActive(recipientId);

            if (configOpt.isEmpty()) {
                log.error("No topic config found for recipientId: {}", recipientId);
                retryHandler.handleFailure(delivery);
                return;
            }

            String topic = configOpt.get().getTopic();

            if (recipientId == solaceRecipientId) {
                // SOLACE FLOW
                TransformedPayloadDto payload = parseTransformedPayload.parseTransformedPayload(transformedPayload);

                FinalPayloadDto finalPayload = finalPayloadMapper.buildFinalPayload(payload, delivery);

                boolean published = publishToSolace.publishToSolace(finalPayload, eventId, topic);

                handleResult(delivery, published, finalPayload, "Solace");

            } else if (recipientId == webhookRecipientId) {
                // WEBHOOK FLOW
                TransformedWebhookPayloadDto payload = parseTransformedPayload.parseWebhookPayload(transformedPayload);

                FinalWebhookPayloadDto finalPayload = finalWebhookPayloadMapper.buildFinalPayload(payload, delivery);

                boolean published = publishToWebhook.publishToSolace(finalPayload, eventId, topic);

                handleResult(delivery, published, finalPayload, "Webhook");


            } else {
                log.error("Failed to send Notification for eventId: {}", eventId);
                retryHandler.handleFailure(delivery);
            }

        } catch (Exception e) {
            log.error("ALERT TO PSS:- Failed to process notification: {}", e.getMessage(), e);
            throw new RuntimeException("Failed to send Notification", e);
        }
    }

    private void handleResult(ConnectNotificationDelivery delivery, boolean published, Object payload, String type) {
        if (published) {
            log.info("{} Notification sent successfully for delivery ID: {}", type, delivery.getId());

            connectNotificationDeliveryRepository.updateStatusById(delivery.getId(), DeliveryStatus.SENT.toString());

            saveFinalPayloadUtil.saveFinalPayload(delivery, payload, type);

        } else {
            log.error("Failed to send {} Notification delivery ID: {}", type, delivery.getId());
            retryHandler.handleFailure(delivery);
        }
    }

}

package com.scb.scroeconnect.solaceworker.config;

import lombok.Getter;
import lombok.Setter;
import org.springframework.boot.context.properties.ConfigurationProperties;
import org.springframework.context.annotation.Configuration;

@Getter
@Setter
@Configuration
@ConfigurationProperties(prefix = "solace.thread")
public class SolaceThreadProperties {
    private int corePoolSize;
    private int maxPoolSize;
    private int queueCapacity;
    private String namePrefix;
    private int cacheSize;
}

package com.scb.scroeconnect.solaceworker.config;

import lombok.Getter;
import lombok.Setter;
import org.springframework.boot.context.properties.ConfigurationProperties;
import org.springframework.context.annotation.Configuration;

import java.util.Map;

@Getter
@Setter
@Configuration
@ConfigurationProperties(prefix = "solace.webhook")
public class WebhookProperties {
    private Map<String, String> queue;
    private Map<String, String> ssl;
    private String topic;

    // --- Initial connection settings ---
    private int connectTimeoutInMillis = 60000;
    private int connectRetries = 10;
    private int connectRetriesPerHost = 5;

    /**
     * Number of times to retry re-establishing a dropped connection after it was live.
     * 60 retries * 3000ms wait = up to 3 minutes of reconnect attempts.
     * Solace handles this at TCP/transport level (different from Spring cache layer).
     */
    private int reconnectRetries = 60;

    /**
     * Wait time in milliseconds between reconnect attempts.
     * Gives the broker time to recover before the next attempt.
     */
    private int reconnectRetryWaitInMillis = 3000;

    /**
     * Interval in milliseconds at which keep-alive heartbeat frames are sent.
     * Detects silently dead connections (e.g. firewall dropping idle TCP without RST).
     * Note: setKeepAliveLimit does NOT exist on SolConnectionFactory API —
     * the missed-heartbeat threshold is configured on the broker side, not the client.
     */
    private int keepAliveIntervalInMillis = 3000;
}


package com.scb.scroeconnect.solaceworker.util;

//import com.fasterxml.jackson.databind.ObjectMapper;
import tools.jackson.databind.ObjectMapper;
import com.scb.scroeconnect.solaceworker.dto.TrailerDto;
import com.scb.scroeconnect.solaceworker.dto.TransformedPayloadDto.TransformedBodyDto;
import com.scb.scroeconnect.solaceworker.dto.TransformedPayloadDto.TransformedHeaderDto;
import com.scb.scroeconnect.solaceworker.dto.TransformedPayloadDto.TransformedPayloadDto;
import com.scb.scroeconnect.solaceworker.entity.*;

import java.util.List;
import java.util.UUID;

public class TestPayloadFactory {
    public static ConnectNotificationDelivery validDelivery() {
        try {
            ObjectMapper mapper = new ObjectMapper();
            String payload = mapper.writeValueAsString(validTransformedPayload());

            ConnectNotificationDelivery d = new ConnectNotificationDelivery();
            d.setId(1L);
            d.setTransformedPayload(payload);

            Recipient r = new Recipient();
            r.setId(1L);
            d.setRecipient(r);

            NotificationEvent e = new NotificationEvent();
            e.setEventId(UUID.randomUUID());
            d.setNotificationEvent(e);

            return d;
        } catch (Exception e) {
            throw new RuntimeException(e);
        }
    }


    private static TransformedPayloadDto validTransformedPayload() {
        TransformedPayloadDto dto = new TransformedPayloadDto();
        dto.setHeader(new TransformedHeaderDto());
        dto.setTrailer(new TrailerDto());

        TransformedBodyDto body =
                new TransformedBodyDto();
        body.setAmount("100");
        body.setTxnDate("2024-01-01T00:00:00");

        dto.setBody(List.of(body));
        return dto;
    }
}

Error: Required type:
JsonNode
Provided:
String
